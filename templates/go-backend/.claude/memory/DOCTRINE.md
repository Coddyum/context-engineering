# Doctrine — <PROJECT_NAME>

Principles that stay true even when the tool (agent, AGENTS.md, bash hooks) changes.
Physical separation: the principle on top (stable), the current implementation below (disposable —
if the tool/stack changes, throw that line away and write a new one; the principle does not move).

> The invariant **architecture** rules (data flow, contracts, module isolation, file sommaire) live
> in `.claude/rules/` and `ARCHITECTURE-BLUEPRINT.md`. This file is for THIS project's durable
> domain/product/security principles as they emerge — do not duplicate the architecture rules here.

## Index

| ID | Principle |
| --- | --- |
| P1 | (add the first durable principle this project commits to) |

## Entries

<!-- Format:

### P[N] : [Principle — phrased as a rule that outlives the tools]

[1-2 sentences describing the rule]
**Why**: [what breaks if it is ignored]
**Current implementation**: [how it translates in THIS repo today — disposable]
-->
